### To run Client end - run the following commands

npm install<br />
npm start<br />
redirect to localhost:3000

### To run the server for the image uploading - run the following commands

npm install<br />
npm start server.js<br />
redirect to localhost:8080
